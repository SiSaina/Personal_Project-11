"use client";
import { useEffect, useState } from "react"; import Navbar from "@/components/Navbar"; import Footer from "@/components/Footer"; import ProductCard from "@/components/ProductCard"; import { useAppContext } from "@/context/AppContext"; import { getBrowsingHistory, getSavedCarts } from "@/services/shopping";
export default function Saved() { const { setCartItems, router } = useAppContext(); const [carts, setCarts] = useState([]); const [history, setHistory] = useState([]); useEffect(() => { let active = true; Promise.all([getSavedCarts(), getBrowsingHistory()]).then(([c, h]) => { if (active) { setCarts(c.data); setHistory(h.data); } }); return () => { active = false; }; }, []); return <><Navbar /><main className="min-h-screen space-y-10 px-6 py-12 md:px-16 lg:px-32"><section><h1 className="text-2xl font-semibold">Saved carts</h1><div className="mt-4 grid gap-3 md:grid-cols-3">{carts.map(c => <button key={c.id} onClick={() => { setCartItems(c.items); router.push('/cart'); }} className="rounded-xl border bg-white p-5 text-left"><strong>{c.name}</strong><p className="text-sm text-gray-500">{Object.keys(c.items).length} products</p></button>)}</div></section><section><h2 className="text-2xl font-semibold">Recently viewed</h2><div className="mt-4 grid grid-cols-2 gap-5 md:grid-cols-5">{history.map(p => <ProductCard key={p.id} product={p} />)}</div></section></main><Footer /></> }
