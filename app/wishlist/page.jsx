"use client";
import { useEffect, useState } from "react"; import Navbar from "@/components/Navbar"; import Footer from "@/components/Footer"; import ProductCard from "@/components/ProductCard"; import Loading from "@/components/Loading"; import { getWishlist } from "@/services/commerce";
export default function Wishlist() { const [products, setProducts] = useState([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); useEffect(() => { let active = true; getWishlist().then(r => active && setProducts(r.data ?? [])).catch(e => active && setError(e.message)).finally(() => active && setLoading(false)); return () => { active = false; }; }, []); return <><Navbar /><main className="min-h-screen px-6 py-12 md:px-16 lg:px-32"><h1 className="mb-8 text-3xl font-semibold">Wishlist</h1>{loading ? <Loading /> : null}{error ? <p className="text-red-700">{error}</p> : null}<div className="grid grid-cols-2 gap-6 md:grid-cols-4">{products.map(p => <ProductCard key={p.id} product={p} />)}</div></main><Footer /></> }
